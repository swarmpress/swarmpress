{
  "name": "@agent-press/dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "CEO dashboard for agent.press",
  "scripts": {
    "dev": "astro dev --port 3001",
    "build": "astro build",
    "preview": "astro preview",
    "clean": "rm -rf dist .astro",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@agent-press/shared": "workspace:*",
    "astro": "^4.2.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
