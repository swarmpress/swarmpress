{
  "name": "@swarm-press/agents",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Agent SDK implementations for swarm.press",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "@swarm-press/backend": "workspace:*",
    "@swarm-press/event-bus": "workspace:*",
    "@swarm-press/shared": "workspace:*",
    "@swarm-press/site-builder": "workspace:*",
    "ajv": "^8.17.1",
    "chrome-launcher": "^1.1.2",
    "dotenv": "^16.3.1",
    "lighthouse": "^12.0.0",
    "linkinator": "^6.1.2",
    "vm2": "^3.9.19",
    "zod": "^3.22.4",
    "zod-to-json-schema": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
