/**
 * Cinque Terre Theme Components
 * Export all components for use in the theme
 */

// React Components (client-side interactive)
export { default as Header } from './Header';
export { default as Footer } from './Footer';
export { default as FeaturedCarousel } from './FeaturedCarousel';

// Re-export types
export * from '../types';
