{
  "name": "@swarm-press/event-bus",
  "version": "0.1.0",
  "private": true,
  "description": "NATS event bus with CloudEvents for swarm.press",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@swarm-press/shared": "workspace:*",
    "dotenv": "^16.3.1",
    "nats": "^2.19.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
